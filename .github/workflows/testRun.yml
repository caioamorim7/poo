name: Test Run

on:
  pull_request:
    branches:
      - 2025.1

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up JDK 21
      uses: actions/setup-java@v2
      with:
        distribution: 'zulu'
        java-version: '21'

    - name: Create lib directory
      run: mkdir -p hello/lib

    - name: Download JUnit Platform Console Standalone
      run: curl -L -o hello/lib/junit-platform-console-standalone-1.11.4.jar https://repo1.maven.org/maven2/org/junit/platform/junit-platform-console-standalone/1.11.4/junit-platform-console-standalone-1.11.4.jar

    - name: Compile Java code
      run: javac -cp "hello/lib/*" -d bin hello/src/*.java hello/test/*.java

    - name: Run tests
      run: java -jar hello/lib/junit-platform-console-standalone-1.11.4.jar --class-path hello/bin --scan-class-path